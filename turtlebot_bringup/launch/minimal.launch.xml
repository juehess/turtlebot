<launch>
  <!-- Turtlebot -->
  <arg name="base" default="$(env TURTLEBOT_BASE)" doc="mobile base type [create, roomba]"/>
  <arg name="battery" default="$(env TURTLEBOT_BATTERY)" doc="kernel provided locatio for battery info, use /proc/acpi/battery/BAT0 in 2.6 or earlier kernels." />
  <arg name="stacks" default="$(env TURTLEBOT_STACKS)" doc="stack type displayed in visualisation/simulation [circles, hexagons]"/>
  <arg name="3d_sensor" default="$(env TURTLEBOT_3D_SENSOR)" doc="3d sensor types [kinect, asux_xtion_pro]"/>
  <arg name="simulation" default="$(env TURTLEBOT_SIMULATION)" doc="set flags to indicate this turtle is run in simulation mode."/>
  <arg name="serialport" default="$(env TURTLEBOT_SERIAL_PORT)" doc="used by create to configure the port it is connected on [/dev/ttyUSB0, /dev/ttyS0]"/>

  <group>
    <include file="$(find-pkg-share turtlebot_bringup)/launch/includes/robot.launch.xml">
      <param name="base" value="$(var base)"/>
      <param name="stacks" value="$(var stacks)"/>
      <param name="3d_sensor" value="$(var 3d_sensor)"/>
    </include>
    <include file="$(find-pkg-share turtlebot_bringup)/launch/includes/mobile_base.launch.xml">
      <param name="base" value="$(var base)" />
      <param name="serialport" value="$(var serialport)" />
    </include>
    <!-- <include unless="$(eval arg('battery') == 'None')" file="$(find turtlebot_bringup)/launch/includes/netbook.launch.xml">
      <param name="battery" value="$(var battery)" />
    </include> -->
  </group>
</launch>
